# Pulse Library Recovery

Recovered from local Cursor checkpoints dated June 3, 2026.

## Run

1. Open a terminal in this folder.
2. Run `./run.sh`
3. Open [http://127.0.0.1:8080](http://127.0.0.1:8080)

## Structure

- `frontend/` contains the newer Pulse Library UI
- `backend/` contains the recovered Java server under `com.pulselibrary`

## Notes

- This recovered version does not require login to generate playlists.
- The app uses Chart.js from a CDN for the stats view.
